/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
 // swcMinify: true, ** on server launch this will throw some kind of warning, kinda annoying
}

module.exports = nextConfig
